alert("string");   -- -this will create one time popup on page saying string typedd.

prompt("string");  ----this will also give the same message, but it can collect input from user from same popup box.

