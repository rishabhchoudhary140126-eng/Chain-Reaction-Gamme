-same as cpp
-can store iff data types
-it's trpe is object
-arrays are mutable.



# Array methods:

push())  : add to end.
unshift() : add element to start.
pop() : return and delete the end element.
shift() : return and delete the first element.
toString() :   converts array to string.
contact() : joins 2 array anf return the result.

slice(startIndx, endIndx) : return part of array
---- splice();
    change original array, can add, remove and replace elements.

    ## syntan:
    splice(startIncex, delCount, newElement,, newElement2, newElement3...);  

    new element added will be on start index.

